{
    "name": "claude-context-bar-fork",
    "displayName": "Claude Context Bar (fork)",
    "description": "Real-time context window usage monitor for Claude Code sessions. Fork: shows only the current window's sessions, hides scheduled tasks, and no longer truncates the list to 5 items silently.",
    "version": "1.8.2",
    "publisher": "andremartins",
    "author": {
        "name": "Ed Zisk",
        "url": "https://github.com/edenaion"
    },
    "contributors": [
        {
            "name": "André Martins",
            "url": "https://github.com/AndreLFSMartins"
        }
    ],
    "license": "MIT",
    "repository": {
        "type": "git",
        "url": "https://github.com/AndreLFSMartins/claude-context-bar"
    },
    "homepage": "https://github.com/AndreLFSMartins/claude-context-bar#readme",
    "bugs": {
        "url": "https://github.com/AndreLFSMartins/claude-context-bar/issues"
    },
    "icon": "images/icon.png",
    "galleryBanner": {
        "color": "#1a1a2e",
        "theme": "dark"
    },
    "keywords": [
        "claude",
        "claude code",
        "anthropic",
        "context window",
        "token counter",
        "ai assistant"
    ],
    "engines": {
        "vscode": "^1.74.0"
    },
    "categories": [
        "Other",
        "Visualization"
    ],
    "activationEvents": [
        "onStartupFinished"
    ],
    "main": "./out/extension.js",
    "contributes": {
        "configuration": {
            "title": "Claude Context Bar",
            "properties": {
                "claudeContextBar.autoColor": {
                    "type": "boolean",
                    "default": true,
                    "description": "Automatically assign different colors to each project (rainbow). When off, uses your base color with subtle variations."
                },
                "claudeContextBar.baseColor": {
                    "type": "string",
                    "enum": [
                        "White",
                        "Blue",
                        "Purple",
                        "Cyan",
                        "Green",
                        "Yellow",
                        "Orange",
                        "Pink"
                    ],
                    "default": "White",
                    "description": "Base color when Auto Color is off. Each project gets a subtle variation of this color."
                },
                "claudeContextBar.showEmoji": {
                    "type": "boolean",
                    "default": true,
                    "description": "Show emoji icons based on project name (music 🎵, code 💻, game 🎮, etc.)"
                },
                "claudeContextBar.contextLimit": {
                    "type": "number",
                    "default": 200000,
                    "description": "Fallback context window size in tokens, used only for unknown or non-Claude model IDs. Current Claude models are auto-detected (1M by default, 200K for Haiku and legacy models)."
                },
                "claudeContextBar.modelContextLimits": {
                    "type": "object",
                    "default": {},
                    "description": "Per-model context limit overrides (Model ID → token limit). Exact match only, no glob or prefix matching. Example: {\"claude-haiku-4-5\": 500000}. Highest priority: overrides auto-detection and the fallback for that exact model."
                },
                "claudeContextBar.warningThreshold": {
                    "type": "number",
                    "default": 50,
                    "description": "Percentage at which to show warning color (yellow)"
                },
                "claudeContextBar.dangerThreshold": {
                    "type": "number",
                    "default": 75,
                    "description": "Percentage at which to show danger color (red)"
                },
                "claudeContextBar.showUsage": {
                    "type": "boolean",
                    "default": false,
                    "description": "Show your Claude subscription usage (the 5-hour session limit from /usage) as a separate item to the right of the context items. Opt-in: reads the Claude Code OAuth token and calls an undocumented Anthropic endpoint, which may stop working at any time. Requires being signed in to Claude Code with a subscription."
                },
                "claudeContextBar.usageWarningThreshold": {
                    "type": "number",
                    "default": 50,
                    "description": "Usage percentage at which to show warning color (yellow) on the usage item"
                },
                "claudeContextBar.usageDangerThreshold": {
                    "type": "number",
                    "default": 75,
                    "description": "Usage percentage at which to show danger color (red) on the usage item"
                },
                "claudeContextBar.usageRefreshInterval": {
                    "type": "number",
                    "default": 60,
                    "description": "How often (in seconds) to refresh subscription usage from the /usage endpoint. The endpoint rate-limits aggressive polling, so avoid very low values."
                },
                "claudeContextBar.refreshInterval": {
                    "type": "number",
                    "default": 30,
                    "description": "Refresh interval in seconds"
                },
                "claudeContextBar.idleTimeout": {
                    "type": "number",
                    "default": 180,
                    "minimum": 0,
                    "maximum": 9999999,
                    "description": "Seconds of inactivity before hiding a session (default: 180 / 3 minutes). Set 0 to never hide idle sessions."
                },
                "claudeContextBar.onlyCurrentWindow": {
                    "type": "boolean",
                    "default": true,
                    "description": "Show only sessions whose working directory is inside this window's workspace folders. Turn off to see sessions from every VS Code window, as upstream does. A window with no folder open always shows everything."
                },
                "claudeContextBar.showScheduledTasks": {
                    "type": "boolean",
                    "default": false,
                    "description": "Show scheduled/background Claude Code runs (sessions whose first message is a <scheduled-task> block) alongside real tabs. Off by default: they are not tabs, and each one takes a status bar slot."
                },
                "claudeContextBar.maxItems": {
                    "type": "number",
                    "default": 12,
                    "minimum": 0,
                    "description": "Maximum number of status bar items. Set 0 for no limit. Upstream hardcoded 5 and dropped the rest with no warning; when this cap truncates the list, a warning is logged to the extension host output."
                },
                "claudeContextBar.tabNameLength": {
                    "type": "number",
                    "default": 6,
                    "minimum": 0,
                    "description": "Characters of the Claude Code tab's own name to show on the status bar item, instead of the project name (default: 6). The Claude Code tab titles itself with your latest prompt, so this is what lets you tell two tabs of the same project apart — the project name only ever says which project. Set 0 to show the project name instead. Sessions that record no prompt fall back to the project name automatically; the color and emoji always stay tied to the project."
                },
                "claudeContextBar.compactMode": {
                    "type": "boolean",
                    "default": false,
                    "description": "Shorten project names to save status bar space. Multi-word names become acronyms (my-cool-project → MCP), single words become abbreviated (typescript → Tscript). Names ≤5 chars stay unchanged."
                },
                "claudeContextBar.shortNames": {
                    "type": "object",
                    "default": {},
                    "description": "Custom short names for projects. Keys are project names, values are the short versions (e.g. {\"my-cool-project\": \"MCP\", \"webapp\": \"WA\"})."
                }
            }
        }
    },
    "scripts": {
        "vscode:prepublish": "npm run compile",
        "compile": "tsc -p ./",
        "watch": "tsc -watch -p ./",
        "lint": "eslint src --ext ts",
        "test": "tsc -p ./ && node --test out/*.test.js",
        "package": "vsce package",
        "publish": "vsce publish"
    },
    "devDependencies": {
        "@types/node": "^18.0.0",
        "@types/vscode": "^1.74.0",
        "typescript": "^5.0.0"
    }
}
